<h5>CLASSES EXISTANTES</h5>
<table class="table table-bordered" id="classes_list">
  <thead><tr>
    <th>#</th>
    <th>Nom de la classe</th>
  </tr></thead>
  <% @categories.each do |category| %>
      <tr>
        <td><%= category.id %></td>
        <td><%= category.name %></td>
      </tr>
  <% end %>
</table><hr>