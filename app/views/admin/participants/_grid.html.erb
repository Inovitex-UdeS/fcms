<h5>Participants existants</h5>
<table class="table table-bordered" id="tableAccompanyists">
  <thead><tr>
    <th>Id</th>
    <th>Nom</th>
    <th>Adresse courriel</th>
    <th>Date de création</th>
  </tr></thead>
  <tbody>
  <% @participants.each do |participant| %>
      <tr>
        <td><%= participant.id %></label></td>
        <td><%= participant.first_name %> <%= participant.last_name %></td>
        <td><%= participant.email %></td>
        <td><%= participant.created_at.strftime("%d/%m/%Y") %></td>
      </tr>
  <% end %>
  </tbody>
</table><hr>