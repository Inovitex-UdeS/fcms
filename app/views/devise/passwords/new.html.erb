<div class="gradient"></div>

<div class="devise-page">
    <div class="devise-left"></div>
    <div class="devise-right">
        <h2>Récupération de mot de passe</h2>

        <p>Vous avez oublié votre mot de passe?</p><p>Entrez votre adresse courriel ci-dessous et les instructions de récupération vous seront envoyées.</p>
        <%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f| %>
          <%= devise_error_messages! %>

          <div class="input-prepend">
            <span class="add-on"><i class="icon-envelope"></i></span>
            <%= f.email_field :email, :autofocus => true, :placeholder => 'exemple@xyz.com', :class => 'input-xlarge' %><br />
          </div>

          <div><%= f.submit "Soumettre", :class => 'btn btn-primary' %></div>
        <% end %>

        <%= render "devise/shared/links" %>
    </div>
</div>